<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chatx.useradmin.dao.UserMapper">

    <insert id="insertOne" keyProperty="id">
        insert into t_user(nickname, mobile, passwd, gender, birthday, area_code, create_at)
        values (#{nickname}, #{mobile}, #{passwd}, #{gender}, #{birthday}, #{areaCode}, #{createAt})
    </insert>
</mapper>
